name: "PR"
on:
  pull_request:

jobs:
  check-format:
    name: "check formatting"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: latest
          cache: npm

      - run: npm install --include dev

      - name: Format files
        run: npm run format

      - name: Check if any files were changed
        run: if [ -n "$(git status --porcelain)" ]; then (exit 1); fi
        shell: bash

  js-up-to-date:
    name: "JS files up-to-date"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: latest
          cache: npm

      - run: npm install --include dev

      - run: npm run clean

      - run: npm run build

      - name: Check if any files were changed
        run: if [ -n "$(git status --porcelain)" ]; then (exit 1); fi
        shell: bash

  test:
    needs: js-up-to-date
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: latest
          cache: npm

      - run: npm install --include dev

      - name: Test
        run: npm run test

